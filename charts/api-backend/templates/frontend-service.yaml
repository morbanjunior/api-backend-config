apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-backend.componentName" (dict "root" $ "component" "frontend") }}
  labels:
    {{- include "api-backend.labels" (dict "root" $ "component" "frontend") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
  selector:
    {{- include "api-backend.selectorLabels" (dict "root" $ "component" "frontend") | nindent 4 }}
